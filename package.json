{
  "name": "geodoodle",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/generate_seo_pages.js",
    "build:portal": "VITE_PORTAL=1 vite build --base ./ --outDir dist-portal",
    "build:yandex": "VITE_PORTAL=1 VITE_PORTAL_TARGET=yandex vite build --base ./ --outDir dist-yandex",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "topojson-client": "^3.1.0",
    "topojson-server": "^3.0.1",
    "topojson-simplify": "^3.0.3",
    "vite": "^8.1.1",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "@fontsource/outfit": "^5.3.0",
    "lucide": "^1.33.0",
    "perfect-freehand": "^1.2.3"
  }
}
