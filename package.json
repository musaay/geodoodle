{
  "name": "geodoodle",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/generate_seo_pages.js",
    "build:portal": "VITE_PORTAL=1 vite build --base ./ --outDir dist-portal",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "vite": "^8.1.1",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "@fontsource/outfit": "^5.3.0",
    "lucide": "^1.33.0",
    "perfect-freehand": "^1.2.3"
  }
}
